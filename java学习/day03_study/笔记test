面向对象特征:
- 封装
    - 在类中封装属性和行为
    - 行为通过成员方法来表示
    - 属性通过成员变量来表示
    - 针对需要保护的属性，加上private关键字
    - 为了方便管理，我们会为所有属性加上private关键字，针对性的对外提供公共的方法
    - 使用get() set()方法 来对外提供公共的方法，是封装中的重要元素
    - this 表示当前类的实例
- 继承
- 多态
面向过程:常见的c语言，因果耦合性比较强
成员变量
- 事物的属性 包括静态方法
- 存放在jvm虚拟机的堆中
局部变量
- 基本数据类型 存放在jvm的栈中
- 引用数据类型，存放在jvm的堆中
引用数据类型会现在栈中开辟内存空间定义一个变量，之后在堆中存放改实例对象的属性方法等，属性方法的数值执行方法区中的内存地址
栈中的变量只想堆中的内存地址。
在java中 万物皆对象，java的核心思想就是创建对象，指挥对象做事情。
通过类创建对象
访问权限修饰符:
- public 公有的 在本工作区中任意位置都可以访问
- private 私有的 仅能在类中进行调用
构造函数（有参和无参）
- 默认系统会带一个无参的构造函数在类中没有构造函数时候
- 作用:在创建对象的时候给对象的属性进行初始化
- 无返回值类型，连void都没有
- 支持方法重载
- 构造函数的名称必须和类相同
- 方法重载
    - 方法名相同，参数列表不同
      - 参数列表:数据类型,数据个数数量